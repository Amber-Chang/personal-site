# Journey Engine
Journey Engine is a platform that manages customer journeys and segmentation.

## Overview

This project provides core infrastructure for tracking and advancing customer journey_id state through configurable segment_rule pipelines.

## Modules

- `src/segmentation` — segment_rule evaluation
- `src/journey` — journey_id lifecycle management
- `src/auth` — auth_token validation middleware
